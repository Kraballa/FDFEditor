<UserControl x:Class="FDFEditor.Control.PatternView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FDFEditor.Control"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <local:TitledTextBox Title="Types" Text="{Binding Path=Types, Mode=TwoWay}"/>
        <local:TitledTextBox Title="GlobalEvents" Text="{Binding Path=GlobalEvents, Mode=TwoWay}"/>
        <local:TitledTextBox Title="Sounds" Text="{Binding Path=Sounds, Mode=TwoWay}"/>
        <local:TitledTextBox Title="Center" Text="{Binding Path=Center, Mode=TwoWay}"/>
        <!--<local:TitledTextBox Title="Total Frames" Text="{Binding Path=TotalFrames, Mode=TwoWay}"/>-->
    </StackPanel>
</UserControl>
