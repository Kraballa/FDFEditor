<UserControl x:Class="FDFEditor.Control.XnaTabItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TreeView x:Name="TreeView">

        </TreeView>
        <GridSplitter Background="DarkGray"  ResizeDirection="Columns" Grid.Column="2" 
                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <ListView x:Name="ViewHolder" Grid.Column="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

        </ListView>
    </Grid>
</UserControl>
